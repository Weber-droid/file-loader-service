# Copy to .env:  cp .env.example .env
# Used by Docker Compose and Spring Boot (no export needed).

DB_HOST=127.0.0.1
DB_PORT=5434
DB_NAME=ussd
DB_USERNAME=postgres
DB_PASSWORD=fileloader

FILE_LOADER_INPUT_DIR=./ussd-inputs
FILE_LOADER_ARCHIVE_DIR=./ussd-inputs/archive
